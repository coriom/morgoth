[pytest]
markers =
    integration: tests that require host services the sandbox does not
        provide (Postgres, ChromaDB volume, real filesystem paths under
        ~/Morgoth). gate_tests inside the sandbox runs
        `-m "not integration"`; the same tests still run on the host.
        HTTP alone does NOT justify this marker — hermetic tests must
        mock HTTP; use pytest-socket to catch violations.
